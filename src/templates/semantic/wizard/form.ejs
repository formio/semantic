<div class="{{ctx.className}}">
  <div class="formio-wizard-position">
    {{ ctx.wizardHeader }}
    <div class="wizard-page" ref="{{wizardKey}}">
      {{ctx.components}}
    </div>
    {{ ctx.wizardNav }}
  </div>
</div>